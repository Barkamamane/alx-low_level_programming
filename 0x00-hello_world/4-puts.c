#include <stdio.h>
/**
 *  Description: main - prints out a string to sdout.
 *  Returns: 0 if succes.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzel");
return (0);
}
